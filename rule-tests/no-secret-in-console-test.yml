id: no-secret-in-console
valid:
  - console.log("fine, nothing here");
  - console.log(user.name, order.id);
invalid:
  - console.log(apiKey);
  - 'console.log("token: " + tok);'
  - console.error(err.password);
  - console.warn(secretValue);
